{
  "name": "blockparty",
  "version": "0.1.0",
  "private": true,
  "description": "Tooling manifest for the blockparty static site. The shipped site remains dependency-free and CDN-loaded; this manifest exists to run the Node test suite and to carry the authoritative release version (see CLAUDE.md § Versioning). Release notes live in CHANGELOG.md.",
  "scripts": {
    "test": "node --test",
    "test:e2e": "node --test e2e/smoke.mjs"
  },
  "devDependencies": {
    "d3": "^7.9.0",
    "d3-geo": "^3.1.1",
    "playwright": "^1.62.1",
    "topojson-client": "^3.1.0"
  }
}
